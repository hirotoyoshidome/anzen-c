# AGENTS.md

このリポジトリで作業するエージェントは、以下の方針に従ってください。

## 基本方針

- すべてのドキュメントは日本語で記述してください。
- 公開用の文書として、主観的な表現、学習メモ調の表現、断定しすぎた表現を避けてください。
- C言語の安全性、可読性、保守性を優先してください。
- このリポジトリは、対象Cプロジェクトの実装置き場ではありません。安全なCコーディング規約、レビュー観点、別プロジェクトに適用するチェック資産を管理する場所です。
- 技術的に不正確または誤解を招く内容を見つけた場合は、そのまま残さず補正してください。
- 規約本文と補足説明を混ぜないでください。強制したいルールは `docs/c-coding-standard.md`、背景説明は `docs/rationale.md` に置きます。

## ドキュメント構成

- `README.md`
  - ANZEN-Cの目的と主要ドキュメントへの導線を書きます。
- `docs/c-coding-standard.md`
  - コーディング規約の本体です。
  - 「必須」「推奨」「禁止」「例外」を明確にします。
- `docs/review-checklist.md`
  - レビューやリファクタリング時に確認する項目です。
  - 実装者とレビュアーが短時間で確認できる粒度にします。
- `docs/rationale.md`
  - 規約の背景、C言語の基礎、補足知識を記述します。
  - 規約本文に入れると長くなる説明を置きます。
- `CLAUDE.md`
  - `AGENTS.md` 参照のみを書きます。
- 将来 `scripts/` を追加する場合
  - 対象Cプロジェクトに対して外部から実行できるチェックを優先します。
  - このリポジトリ内にサンプル以上のC実装を増やすことは避けます。
  - チェック対象、入力、出力、終了コードを明確にします。

## 文体

- 規約本文では、次の語を使い分けてください。
  - `必須`: 原則として守る必要があるルール。
  - `推奨`: 事情がなければ採用するルール。
  - `禁止`: 採用してはいけないルール。
  - `例外`: 明確な理由がある場合に限り認める扱い。
- 箇条書きは短く保ち、1項目に複数の論点を詰め込まないでください。
- 用語はできるだけ統一してください。
  - `NULL`
  - `size_t`
  - `malloc`
  - `realloc`
  - `free`
  - `ヘッダファイル`
  - `ソースファイル`
  - `ESP-IDF`
  - `FreeRTOS`
- Markdownのリンクは相対パスで記述してください。
- 見出し階層は飛ばさないでください。

## 内容更新時の注意

- `docs/c-coding-standard.md` を更新した場合は、必要に応じて `docs/review-checklist.md` も更新してください。
- 新しい背景説明を追加する場合は、規約本文ではなく `docs/rationale.md` に置くことを優先してください。
- `README.md` のリンク切れや読む順序が古くなっていないか確認してください。
- C標準、POSIX、GCC固有機能、ESP-IDF、FreeRTOS、ターゲット依存APIの区別が曖昧にならないようにしてください。
- `wchar_t`、文字エンコーディング、整数サイズ、ポインタサイズなど環境依存のある内容は、環境依存であることを明記してください。
- `static`、`extern`、`const`、`volatile`、`restrict` などの修飾子は、用途を限定して説明してください。
- 組み込み環境では、ヒープ使用量、スタック使用量、割り込みコンテキスト、ログ出力負荷、ブロッキング処理を考慮してください。
- 抽象化は単なる便利化ではなく、制約によって安全性を高める設計として扱ってください。
- 過剰な抽象化を推奨しない一方で、並列に増える機能や所有権を固定したい領域では、インターフェイスを明確にした抽象化を検討してください。

## コード例の扱い

- コード例は最小限にしてください。
- 未検証のコードを完成例として載せないでください。
- 例示コードでは、エラー処理、所有権、解放責務が読み取れるようにしてください。
- 危険な例を載せる場合は、危険である理由を明確にしてください。

## 作業後の確認

- `rg --files` で作成・更新したファイルを確認してください。
- Markdown内の相対リンクが存在するファイルを指していることを確認してください。
- ルート直下に `README.md`, `AGENTS.md`, `CLAUDE.md` が存在することを確認してください。
- `docs/` 配下に規約本文、補足、チェックリストが存在することを確認してください。
